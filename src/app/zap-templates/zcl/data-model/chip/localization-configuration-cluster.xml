<?xml version="1.0"?>
<configurator>
  <cluster>
    <attribute code="0x00" define="ACTIVE_LOCALE" length="35" optional="false" side="server" type="char_string" writable="true">
      <access op="write" privilege="manage"/>
      <description>ActiveLocale</description>
    </attribute>
    <attribute code="0x01" define="SUPPORTED_LOCALES" entryType="char_string" length="254" optional="false" side="server" type="ARRAY" writable="false">SupportedLocales</attribute>
    <client init="false" tick="false">true</client>
    <code>0x002b</code>
    <define>LOCALIZATION_CONFIGURATION_CLUSTER</define>
    <description>Nodes should be expected to be deployed to any and all regions of the world. These global regions
      may have differing common languages, units of measurements, and numerical formatting
      standards. As such, Nodes that visually or audibly convey information need a mechanism by which
      they can be configured to use a user’s preferred language, units, etc</description>
    <domain>General</domain>
    <name>Localization Configuration</name>
    <server init="false" tick="false">true</server>
  </cluster>
  <domain name="CHIP"/>
</configurator>
